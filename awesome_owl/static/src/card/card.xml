<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t t-name="awesome_owl.Card">
        <div class="card d-inline-block m-2 align-top shadow-sm" style="width: 18rem;">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h5 class="card-title mb-0"><t t-esc="props.title"/></h5>
                <button class="btn btn-sm btn-outline-secondary" t-on-click="toggle">
                    <i t-att-class="state.isOpen ? 'fa fa-chevron-up' : 'fa fa-chevron-down'"/>
                </button>
            </div>
            <div t-if="state.isOpen" class="card-body">
                <t t-slot="default"/>
            </div>
        </div>
    </t>

</templates>
