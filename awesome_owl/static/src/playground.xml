<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

    <t t-name="awesome_owl.playground">
        <div class="p-3">
            hello world
            <Counter value="counters[0]" onChange.bind="() => this.counters[0]++" />
            <Counter value="counters[1]" onChange.bind="() => this.counters[1]++" />
            <Counter value="counters[1]" onChange.bind="() => this.counters[1]++" />
            <div>The sum is: <t t-esc="sum"/></div>
        </div>
        <div>
            <Card title="'card 1'">
                content of card 1
            </Card>
            <Card title="'card 2'">
              <Counter value="counters[0]" onChange.bind="() => this.counters[0]++" />
            </Card>
        </div>
        <TodoList />
    </t>

</templates>
